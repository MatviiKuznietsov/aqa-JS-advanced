function getArea(age) {
    return age >= 18
}

const age1 = 15
const age2 = 25
console.log(`Man adult with age ${age1} = ${getArea(age1)}`)
console.log(`Man adult with age ${age2} = ${getArea(age2)}`)
